***React JS***
virtual DOM
Asynchronous JS (one task at a time)
Parent to child (unidirectional)
=>>npx create-react-app
filename first word capital 
make a component and import it to use it
==>> npm install 
when used on another system so use upper command to install all necessary files for npm
=>>Component types
1. functional component 2. class component 3. Pure component 4. high-order component 5. controlled component
6. uncontrolled component
* Watch Git repository Code4Education where explained about default export function and  export function and  also 
& how to call them for reuse in the same component and different component. i.e, <NewUser/>
*function within function accessed as below but cannot use outside of the function
{common()}
<common />
